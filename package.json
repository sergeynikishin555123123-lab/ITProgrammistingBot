{
  "name": "codefarm",
  "version": "1.0.0",
  "description": "CodeFarm - игровая платформа для обучения программированию",
  "scripts": {
    "build": "echo 'Python project - no build step needed'",
    "start": "pm2 start ecosystem.config.js --no-daemon",
    "install-python-deps": "python -m pip install -r backend/requirements.txt"
  },
  "dependencies": {
    "pm2": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}
