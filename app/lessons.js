class Lessons {
    constructor() {
        this.lessons = this.loadLessons();
    }
    
    loadLessons() {
        return [
            // УРОКИ 1-10: ОСНОВЫ PYTHON
            {
                id: "lesson_1",
                title: "Первые команды боту-помощнику",
                description: "Научись давать базовые команды боту для подготовки территории фермы",
                level: 1,
                experience: 100,
                coins: 50,
                theory: `
                    <h2>👋 Привет, фермер!</h2>
                    <p>Я - твой умный помощник <strong>АгроБот</strong>. Я понимаю команды на Python и могу управлять всей техникой на ферме.</p>
                    
                    <h3>🎯 Что такое функция?</h3>
                    <p>Функция в Python - это как кнопка на пульте управления. Нажал - получил результат.</p>
                    
                    <pre><code># Примеры команд для меня:
print("Привет!")              # - я напечатаю текст
bot_say("Начинаю работу")     # - я произнесу это вслух
clear_area("север")           # - расчищу участок</code></pre>
                    
                    <h3>✏️ Твоя первая программа</h3>
                    <p>Ты не просто "пишешь код", а составляешь инструкцию для умного помощника!</p>
                `,
                task: "Напиши программу, которая поприветствует бота и попросит его расчистить участок под строительство.",
                exampleCode: `# Пример базовых команд
print("Привет, АгроБот!")
bot_say("Начинаю работу")
clear_area("north")
clear_area("south")`,
                initialCode: `# Напиши свой код здесь
# 1. Поприветствуй бота
# 2. Попроси расчистить участок`,
                checks: [
                    { keyword: "print", points: 30, error: "Используй print() для вывода сообщений" },
                    { keyword: "bot_say", points: 40, error: "Используй bot_say() для общения с ботом" },
                    { keyword: "clear_area", points: 30, error: "Используй clear_area() для расчистки территории" }
                ],
                requiredAction: "clear_area",
                farmUpdate: {
                    action: "clear_land",
                    landCleared: true,
                    visualEffect: "clearing_animation",
                    resources: { water: 100, energy: 100, coins: 50 }
                }
            },
            {
                id: "lesson_2",
                title: "Переменные - Проект фермы",
                description: "Используй переменные для создания проекта своей фермы",
                level: 1,
                experience: 150,
                coins: 75,
                theory: `
                    <h2>📦 Что такое переменная?</h2>
                    <p>Переменная - это как ящик с надписью. Положил что-то внутрь, подписал - потом легко найдешь.</p>
                    
                    <pre><code># Создаем переменные-ящики:
farm_name = "Солнечная долина"  # Текст (строка)
area = 100                       # Число
has_water = True                 # Логическое значение (Да/Нет)</code></pre>
                    
                    <h3>📊 Типы данных</h3>
                    <ul>
                        <li><strong>str</strong> (текст): "дерево", "кирпич"</li>
                        <li><strong>int</strong> (числа): 10, 5, 100</li>
                        <li><strong>bool</strong> (логика): True, False</li>
                    </ul>
                    
                    <h3>🎨 Создай свой проект</h3>
                    <p>Используй переменные чтобы спроектировать ферму своей мечты!</p>
                `,
                task: "Создай переменные для названия фермы, площади, количества животных и построй первый дом.",
                exampleCode: `# Создание переменных
farm_name = "Солнечная долина"
area = 100  # гектаров
animals = 10
build_house("деревянный", 2)`,
                initialCode: `# Создай проект фермы
# 1. Название фермы
# 2. Площадь
# 3. Количество животных
# 4. Построй дом`,
                checks: [
                    { keyword: "farm_name", points: 25, error: "Создай переменную farm_name" },
                    { keyword: "area", points: 25, error: "Создай переменную area" },
                    { keyword: "animals", points: 25, error: "Создай переменную animals" },
                    { keyword: "build_house", points: 25, error: "Используй build_house() для постройки" }
                ],
                farmUpdate: {
                    action: "build_house",
                    buildingType: "house",
                    position: { x: 0, z: 0 },
                    visualEffect: "building_animation",
                    resources: { wood: -50, coins: -100, house_built: 1 }
                }
            },
            {
                id: "lesson_3",
                title: "Функции - Расчистка территории",
                description: "Создай свои функции для управления техникой фермы",
                level: 1,
                experience: 200,
                coins: 100,
                theory: `
                    <h2>🔧 Что такое функция?</h2>
                    <p>Функция - это именованный блок кода, который можно вызывать много раз.</p>
                    
                    <pre><code># Создание функции
def say_hello():
    print("Привет, фермер!")
    bot_say("Рад помочь")

# Вызов функции
say_hello()  # Функция выполнится</code></pre>
                    
                    <h3>🎯 Зачем нужны функции?</h3>
                    <ul>
                        <li><strong>Повторное использование</strong> - не копируй код, вызывай функцию</li>
                        <li><strong>Упорядочивание</strong> - сложную задачу разбей на маленькие функции</li>
                        <li><strong>Читаемость</strong> - понятные названия функций заменяют комментарии</li>
                    </ul>
                    
                    <h3>🚜 Пример для фермы</h3>
                    <p>Вместо повторения одних и тех же команд для каждой техники, создай функции управления!</p>
                `,
                task: "Создай функцию для запуска трактора и функцию для расчистки участка. Используй их для подготовки поля.",
                exampleCode: `# Создай свои функции
def start_tractor():
    print("Запускаю трактор...")
    bot_say("Двигатель работает")
    return "Трактор готов"

def clear_section(section_name):
    print(f"Расчищаю {section_name}")
    bot_say(f"Участок {section_name} очищен")
    return True

# Используй функции
start_tractor()
clear_section("северный")`,
                initialCode: `# Создай свои функции
# 1. Функция запуска трактора
# 2. Функция расчистки участка
# 3. Используй их для работы`,
                checks: [
                    { keyword: "def ", points: 40, error: "Используй def для создания функции" },
                    { keyword: "start_tractor", points: 30, error: "Создай функцию start_tractor" },
                    { keyword: "clear_section", points: 30, error: "Создай функцию clear_section" }
                ],
                farmUpdate: {
                    action: "prepare_field",
                    fieldSize: "medium",
                    tractorActive: true,
                    visualEffect: "tractor_working",
                    resources: { fuel: -20, energy: -30, field_prepared: true }
                }
            },
            {
                id: "lesson_4",
                title: "Аргументы функций - Строительство дома",
                description: "Используй аргументы функций для кастомизации построек",
                level: 2,
                experience: 250,
                coins: 125,
                theory: `
                    <h2>🎨 Аргументы функций</h2>
                    <p>Аргументы позволяют передавать данные внутрь функции. Это как дать строителю чертеж с размерами.</p>
                    
                    <pre><code># Функция с аргументами
def build_house(material, floors, color):
    print(f"Строю дом: {floors} этажа, {material}, цвет {color}")
    return "Дом построен"

# Использование с разными аргументами
house1 = build_house("дерево", 1, "коричневый")
house2 = build_house("кирпич", 2, "красный")</code></pre>
                    
                    <h3>📐 Типы аргументов</h3>
                    <ul>
                        <li><strong>Обязательные</strong> - должны быть переданы всегда</li>
                        <li><strong>Со значением по умолчанию</strong> - можно не передавать</li>
                        <li><strong>Именованные</strong> - передаются по имени</li>
                    </ul>
                `,
                task: "Создай функцию для постройки дома, которая принимает параметры: материал, этажи, цвет. Построй дом по своему проекту.",
                exampleCode: `# Функция с аргументами
def build_house(material, floors=1, color="белый"):
    print(f"Строю {floors}-этажный {material} дом")
    print(f"Цвет: {color}")
    bot_say("Строительство начато")
    return f"Дом из {material} построен"

# Разные дома
my_house = build_house("кирпич", 2, "красный")
guest_house = build_house("дерево")  # Использует значения по умолчанию`,
                initialCode: `# Создай функцию постройки дома
# Добавь аргументы: материал, этажи, цвет
# Построй свой дом`,
                checks: [
                    { keyword: "def build_house", points: 30, error: "Создай функцию build_house" },
                    { keyword: "material", points: 25, error: "Добавь аргумент material" },
                    { keyword: "floors", points: 25, error: "Добавь аргумент floors" },
                    { keyword: "color", points: 20, error: "Добавь аргумент color" }
                ],
                farmUpdate: {
                    action: "custom_build",
                    building: "house",
                    customization: { material: "brick", floors: 2, color: "red" },
                    visualEffect: "custom_building",
                    resources: { bricks: -200, coins: -250, house_count: 1 }
                }
            },
            {
                id: "lesson_5",
                title: "Списки - План посадки",
                description: "Используй списки для планирования посадок на ферме",
                level: 2,
                experience: 300,
                coins: 150,
                theory: `
                    <h2>📋 Что такое список?</h2>
                    <p>Список (list) - это коллекция элементов в определенном порядке.</p>
                    
                    <pre><code># Создание списка
vegetables = ["морковь", "картофель", "лук", "помидоры"]
numbers = [1, 2, 3, 4, 5]
mixed = ["яблоко", 10, True, 3.14]

# Доступ к элементам
first_veg = vegetables[0]  # "морковь"
last_veg = vegetables[-1]  # "помидоры"</code></pre>
                    
                    <h3>🔧 Основные операции</h3>
                    <ul>
                        <li><strong>Добавление:</strong> vegetables.append("огурцы")</li>
                        <li><strong>Удаление:</strong> vegetables.remove("лук")</li>
                        <li><strong>Длина:</strong> len(vegetables)</li>
                        <li><strong>Проверка:</strong> "морковь" in vegetables</li>
                    </ul>
                `,
                task: "Создай список культур для посадки, список инструментов и реализуй план посадки.",
                exampleCode: `# Работа со списками на ферме
crops_to_plant = ["пшеница", "кукуруза", "картофель"]
tools_needed = ["лопата", "грабли", "лейка"]

print(f"Сегодня сажаю: {crops_to_plant}")
print(f"Инструменты: {tools_needed}")

# Посадка каждой культуры
for crop in crops_to_plant:
    plant(crop)
    print(f"Посажена {crop}")

# Добавление новой культуры
crops_to_plant.append("морковь")
print(f"Новый план: {crops_to_plant}")`,
                initialCode: `# Создай списки для фермы
# 1. Список культур
# 2. Список инструментов
# 3. Реализуй посадку`,
                checks: [
                    { keyword: "[", points: 30, error: "Используй квадратные скобки для списка" },
                    { keyword: "crops", points: 25, error: "Создай список crops" },
                    { keyword: "tools", points: 25, error: "Создай список tools" },
                    { keyword: "append", points: 20, error: "Используй append() для добавления" }
                ],
                farmUpdate: {
                    action: "plant_crops",
                    crops: ["wheat", "corn", "potato"],
                    fieldPositions: ["field_1", "field_2", "field_3"],
                    visualEffect: "planting_animation",
                    resources: { seeds: -30, water: -50, crops_planted: 3 }
                }
            },
            {
                id: "lesson_6",
                title: "Циклы for - Автоматическая посадка",
                description: "Автоматизируй посадку с помощью циклов",
                level: 3,
                experience: 350,
                coins: 175,
                theory: `
                    <h2>🔄 Цикл for</h2>
                    <p>Цикл for выполняет блок кода для каждого элемента в последовательности.</p>
                    
                    <pre><code># Простой цикл for
for i in range(5):
    print(f"Посадка {i+1}")
    plant_seed()

# Цикл по списку
vegetables = ["морковь", "редис", "свекла"]
for veg in vegetables:
    print(f"Сажаю {veg}")
    plant(veg)</code></pre>
                    
                    <h3>🎯 Функция range()</h3>
                    <p>range() создает последовательность чисел:</p>
                    <ul>
                        <li>range(5) → 0, 1, 2, 3, 4</li>
                        <li>range(2, 6) → 2, 3, 4, 5</li>
                        <li>range(0, 10, 2) → 0, 2, 4, 6, 8</li>
                    </ul>
                `,
                task: "Используй цикл for для автоматической посадки 10 грядок моркови и 5 грядок картофеля.",
                exampleCode: `# Автоматическая посадка с циклами
print("Начинаю посадку моркови:")
for i in range(10):
    plant_carrot()
    print(f"Морковь #{i+1} посажена")
    water()  # Поливаем каждую грядку

print("\nНачинаю посадку картофеля:")
for potato_num in range(5):
    plant_potato()
    print(f"Картофель #{potato_num+1} готов")
    
print("🎉 Все посадки завершены!")`,
                initialCode: `# Используй циклы for
# 1. Посади 10 грядок моркови
# 2. Посади 5 грядок картофеля
# 3. Поливай каждую грядку`,
                checks: [
                    { keyword: "for ", points: 40, error: "Используй цикл for" },
                    { keyword: "range(10)", points: 30, error: "Используй range(10) для 10 повторений" },
                    { keyword: "range(5)", points: 30, error: "Используй range(5) для 5 повторений" }
                ],
                farmUpdate: {
                    action: "auto_plant",
                    cropsPlanted: 15,
                    fieldType: "organized",
                    visualEffect: "auto_planting",
                    resources: { seeds: -45, water: -75, energy: -40, automation: 10 }
                }
            },
            {
                id: "lesson_7",
                title: "Условия if-else - Умная система полива",
                description: "Создай умную систему полива, которая реагирует на условия",
                level: 3,
                experience: 400,
                coins: 200,
                theory: `
                    <h2>🤔 Условные операторы</h2>
                    <p>Операторы if/elif/else позволяют программе принимать решения.</p>
                    
                    <pre><code># Простое условие
moisture = 30
if moisture < 20:
    print("Почва сухая!")
    water_heavily()
elif moisture < 40:
    print("Почва нормальная")
    water_lightly()
else:
    print("Почва влажная")
    # Не поливаем</code></pre>
                    
                    <h3>⚖️ Операторы сравнения</h3>
                    <ul>
                        <li><strong>==</strong> равно</li>
                        <li><strong>!=</strong> не равно</li>
                        <li><strong>></strong> больше</li>
                        <li><strong><</strong> меньше</li>
                        <li><strong>>=</strong> больше или равно</li>
                        <li><strong><=</strong> меньше или равно</li>
                    </ul>
                `,
                task: "Создай систему, которая проверяет влажность почвы и решает, нужно ли поливать растения.",
                exampleCode: `# Умная система полива
def smart_watering(moisture_level):
    if moisture_level < 20:
        print("🔴 КРИТИЧЕСКИ СУХО!")
        bot_say("Срочный полив!")
        water(amount=100)  # Обильный полив
        return "Обильный полив"
    
    elif moisture_level < 40:
        print("🟡 Суховато")
        bot_say("Стандартный полив")
        water(amount=50)   # Стандартный полив
        return "Стандартный полив"
    
    elif moisture_level < 70:
        print("🟢 Норма")
        water(amount=20)   # Легкий полив
        return "Легкий полив"
    
    else:
        print("🔵 Влажно")
        bot_say("Полив не требуется")
        return "Не поливать"

# Тестируем систему
test1 = smart_watering(15)  # 🔴
test2 = smart_watering(35)  # 🟡
test3 = smart_watering(80)  # 🔵`,
                initialCode: `# Создай умную систему полива
# 1. Проверь влажность почвы
# 2. Если меньше 20% - обильный полив
# 3. Если 20-40% - легкий полив
# 4. Если больше 40% - не поливать`,
                checks: [
                    { keyword: "if ", points: 30, error: "Используй оператор if" },
                    { keyword: "elif ", points: 25, error: "Используй elif для дополнительных условий" },
                    { keyword: "else:", points: 25, error: "Используй else для остальных случаев" },
                    { keyword: "<", points: 20, error: "Используй оператор сравнения" }
                ],
                farmUpdate: {
                    action: "smart_watering",
                    waterUsed: "calculated",
                    moistureLevel: "optimal",
                    visualEffect: "smart_water",
                    resources: { water: -60, energy: -20, efficiency: 30 }
                }
            },
            {
                id: "lesson_8",
                title: "Словари - База данных растений",
                description: "Создай базу данных растений с характеристиками",
                level: 4,
                experience: 450,
                coins: 225,
                theory: `
                    <h2>📚 Что такое словарь?</h2>
                    <p>Словарь (dict) хранит данные в виде пар ключ-значение.</p>
                    
                    <pre><code># Создание словаря
plant = {
    "name": "морковь",
    "growth_days": 80,
    "water_needed": "medium",
    "season": "spring"
}

# Доступ к значениям
print(plant["name"])          # "морковь"
print(plant["growth_days"])   # 80

# Добавление нового ключа
plant["color"] = "оранжевый"</code></pre>
                    
                    <h3>🔑 Основные операции</h3>
                    <ul>
                        <li><strong>Получение:</strong> plant.get("name")</li>
                        <li><strong>Проверка:</strong> "name" in plant</li>
                        <li><strong>Удаление:</strong> del plant["season"]</li>
                        <li><strong>Все ключи:</strong> plant.keys()</li>
                        <li><strong>Все значения:</strong> plant.values()</li>
                    </ul>
                `,
                task: "Создай словарь с данными о растениях: название, срок созревания, потребность в воде. Добавь функцию поиска информации.",
                exampleCode: `# База данных растений
plants_database = {
    "морковь": {
        "growth_days": 80,
        "water": "medium",
        "season": "spring",
        "yield": "high"
    },
    "пшеница": {
        "growth_days": 120,
        "water": "high",
        "season": "autumn",
        "yield": "medium"
    },
    "картофель": {
        "growth_days": 100,
        "water": "medium",
        "season": "spring",
        "yield": "high"
    }
}

# Функция поиска информации
def get_plant_info(plant_name):
    if plant_name in plants_database:
        info = plants_database[plant_name]
        print(f"🌱 {plant_name}:")
        print(f"   📅 Созревает за {info['growth_days']} дней")
        print(f"   💧 Полив: {info['water']}")
        print(f"   🍂 Сезон: {info['season']}")
        return info
    else:
        print(f"❌ {plant_name} нет в базе данных")
        return None

# Использование
carrot_info = get_plant_info("морковь")
wheat_info = get_plant_info("пшеница")`,
                initialCode: `# Создай базу данных растений
# 1. Словарь с данными
# 2. Добавь несколько растений
# 3. Реализуй поиск информации`,
                checks: [
                    { keyword: "{", points: 30, error: "Используй фигурные скобки для словаря" },
                    { keyword: ":", points: 25, error: "Используй двоеточие между ключом и значением" },
                    { keyword: "plants_database", points: 25, error: "Создай словарь plants_database" },
                    { keyword: "in ", points: 20, error: "Используй in для проверки наличия ключа" }
                ],
                farmUpdate: {
                    action: "database_created",
                    plantsInDb: 5,
                    knowledgeLevel: "increased",
                    visualEffect: "database_animation",
                    resources: { coins: 50, knowledge: 100 }
                }
            },
            {
                id: "lesson_9",
                title: "Циклы while - Автоматический сбор урожая",
                description: "Используй циклы while для непрерывного сбора урожая",
                level: 4,
                experience: 500,
                coins: 250,
                theory: `
                    <h2>🔄 Цикл while</h2>
                    <p>Цикл while выполняет блок кода, пока условие истинно.</p>
                    
                    <pre><code># Простой цикл while
count = 0
while count < 5:
    print(f"Счет: {count}")
    count += 1  # Увеличиваем счетчик

# Бесконечный цикл (осторожно!)
# while True:
#     print("Это будет печататься вечно!")</code></pre>
                    
                    <h3>🎯 Когда использовать while?</h3>
                    <ul>
                        <li>Когда не знаешь точное количество повторений</li>
                        <li>Пока есть данные для обработки</li>
                        <li>Ожидание какого-то события</li>
                        <li>Игры и симуляции</li>
                    </ul>
                    
                    <h3>⚠️ Важно!</h3>
                    <p>Всегда убедись, что условие цикла когда-нибудь станет ложным, иначе получишь бесконечный цикл!</p>
                `,
                task: "Создай систему автоматического сбора урожая, которая работает, пока есть созревшие культуры.",
                exampleCode: `# Автоматический сбор урожая
ripe_crops = 10  # Начальное количество созревших культур
harvested = 0    # Собранные культуры

print("🚜 Начинаю автоматический сбор урожая...")

while ripe_crops > 0:
    harvest()  # Собираем одну культуру
    ripe_crops -= 1  # Уменьшаем количество созревших
    harvested += 1   # Увеличиваем счетчик собранных
    
    print(f"📦 Собрано: {harvested}. Осталось: {ripe_crops}")
    
    # Небольшая пауза между сбором
    wait(1)

print(f"🎉 Урожай собран! Всего: {harvested} культур")

# Дополнительно: сбор с проверкой готовности
crops_in_field = ["пшеница", "морковь", "картофель", "пшеница"]
index = 0

while index < len(crops_in_field):
    crop = crops_in_field[index]
    if is_ripe(crop):
        harvest_crop(crop)
        print(f"Собрана {crop}")
    index += 1`,
                initialCode: `# Создай систему сбора урожая
# 1. Начни с 10 созревших культур
# 2. Пока есть культуры - собирай
# 3. После сбора всех - сообщи об окончании`,
                checks: [
                    { keyword: "while ", points: 40, error: "Используй цикл while" },
                    { keyword: "ripe_crops", points: 30, error: "Используй переменную ripe_crops" },
                    { keyword: "harvest", points: 30, error: "Используй функцию harvest()" }
                ],
                farmUpdate: {
                    action: "auto_harvest",
                    cropsHarvested: 10,
                    harvestSpeed: "continuous",
                    visualEffect: "harvesting_loop",
                    resources: { crops: 10, coins: 100, energy: -80, efficiency: 40 }
                }
            },
            {
                id: "lesson_10",
                title: "Функции с возвратом значений - Расчет прибыли",
                description: "Создай функции, которые возвращают результаты расчетов",
                level: 5,
                experience: 550,
                coins: 275,
                theory: `
                    <h2>📊 Возврат значений</h2>
                    <p>Функции могут возвращать результаты с помощью оператора return.</p>
                    
                    <pre><code># Функция, которая возвращает значение
def calculate_area(width, height):
    area = width * height
    return area

# Использование возвращаемого значения
my_area = calculate_area(10, 5)
print(f"Площадь: {my_area}")  # 50

# Можно сразу использовать
print(f"Площадь 2: {calculate_area(7, 3)}")  # 21</code></pre>
                    
                    <h3>🎯 Зачем возвращать значения?</h3>
                    <ul>
                        <li><strong>Переиспользование</strong> - результат можно использовать в разных местах</li>
                        <li><strong>Комбинирование</strong> - результаты одних функций можно передавать в другие</li>
                        <li><strong>Тестирование</strong> - легко проверить правильность работы</li>
                        <li><strong>Читаемость</strong> - код становится более понятным</li>
                    </ul>
                `,
                task: "Создай функцию, которая рассчитывает прибыль от продажи урожая, учитывая затраты на семена и уход.",
                exampleCode: `# Функция расчета прибыли
def calculate_profit(harvest_kg, price_per_kg, seed_cost, care_cost):
    """
    Рассчитывает прибыль от продажи урожая
    harvest_kg - урожай в килограммах
    price_per_kg - цена за килограмм
    seed_cost - затраты на семена
    care_cost - затраты на уход
    """
    # Выручка от продажи
    revenue = harvest_kg * price_per_kg
    
    # Общие затраты
    total_cost = seed_cost + care_cost
    
    # Прибыль
    profit = revenue - total_cost
    
    return profit

# Использование функции
wheat_profit = calculate_profit(
    harvest_kg=100,      # 100 кг пшеницы
    price_per_kg=50,     # 50 монет за кг
    seed_cost=200,       # 200 монет за семена
    care_cost=100        # 100 монет за уход
)

print(f"💰 Прибыль от пшеницы: {wheat_profit} монет")

# Расчет для разных культур
crops = [
    {"name": "морковь", "harvest": 80, "price": 40, "seeds": 150, "care": 80},
    {"name": "картофель", "harvest": 120, "price": 30, "seeds": 200, "care": 100}
]

for crop in crops:
    profit = calculate_profit(
        crop["harvest"],
        crop["price"],
        crop["seeds"],
        crop["care"]
    )
    print(f"{crop['name']}: {profit} монет прибыли")`,
                initialCode: `# Создай функцию расчета прибыли
# 1. Принимает: урожай, цена, затраты на семена, затраты на уход
# 2. Возвращает прибыль
# 3. Протестируй на разных культурах`,
                checks: [
                    { keyword: "def ", points: 30, error: "Используй def для создания функции" },
                    { keyword: "return ", points: 40, error: "Используй return для возврата значения" },
                    { keyword: "calculate_profit", points: 30, error: "Создай функцию calculate_profit" }
                ],
                farmUpdate: {
                    action: "profit_calculation",
                    profitMade: 500,
                    financialSkill: "improved",
                    visualEffect: "money_animation",
                    resources: { coins: 200, knowledge: 50, business: 100 }
                }
            },

            // УРОКИ 11-20: ПРОДВИНУТЫЕ ОСНОВЫ
            {
                id: "lesson_11",
                title: "Обработка ошибок - Система оповещений",
                description: "Научись обрабатывать ошибки в программах фермы",
                level: 5,
                experience: 600,
                coins: 300,
                theory: `
                    <h2>🛡️ Обработка ошибок</h2>
                    <p>Программы могут сталкиваться с непредвиденными ситуациями. Обработка ошибок помогает им работать стабильно.</p>
                    
                    <pre><code># Безопасное выполнение с try-except
try:
    # Код, который может вызвать ошибку
    result = 10 / 0
    print(result)
except ZeroDivisionError:
    # Что делать при ошибке деления на ноль
    print("Ошибка: деление на ноль!")

# Программа продолжает работать
print("Программа завершена")</code></pre>
                    
                    <h3>🎯 Типы ошибок</h3>
                    <ul>
                        <li><strong>SyntaxError</strong> - ошибка синтаксиса</li>
                        <li><strong>ZeroDivisionError</strong> - деление на ноль</li>
                        <li><strong>TypeError</strong> - несовместимые типы</li>
                        <li><strong>ValueError</strong> - неправильное значение</li>
                        <li><strong>FileNotFoundError</strong> - файл не найден</li>
                    </ul>
                `,
                task: "Создай безопасную систему запуска трактора с обработкой возможных ошибок.",
                exampleCode: `# Безопасный запуск трактора
def safe_start_tractor(fuel_level, engine_status):
    try:
        print("🚜 Пытаюсь запустить трактор...")
        
        # Проверяем топливо
        if fuel_level <= 0:
            raise ValueError("❌ Нет топлива!")
        
        # Проверяем двигатель
        if engine_status != "working":
            raise RuntimeError("❌ Двигатель неисправен!")
        
        # Запускаем
        print("✅ Трактор успешно запущен!")
        return True
        
    except ValueError as fuel_error:
        print(f"🛠️ Проблема с топливом: {fuel_error}")
        print("💡 Решение: заправьте трактор")
        return False
        
    except RuntimeError as engine_error:
        print(f"🛠️ Проблема с двигателем: {engine_error}")
        print("💡 Решение: вызовите механика")
        return False
        
    except Exception as unexpected_error:
        print(f"⚠️ Неожиданная ошибка: {unexpected_error}")
        return False

# Тестируем систему
print("Тест 1 - Нормальный запуск:")
safe_start_tractor(fuel_level=50, engine_status="working")

print("\nТест 2 - Нет топлива:")
safe_start_tractor(fuel_level=0, engine_status="working")

print("\nТест 3 - Сломан двигатель:")
safe_start_tractor(fuel_level=50, engine_status="broken")`,
                initialCode: `# Создай безопасную систему запуска
# 1. Проверь наличие топлива
# 2. Проверь состояние двигателя
# 3. Обработай возможные ошибки`,
                checks: [
                    { keyword: "try:", points: 30, error: "Используй try для безопасного выполнения" },
                    { keyword: "except ", points: 30, error: "Используй except для обработки ошибок" },
                    { keyword: "raise ", points: 20, error: "Используй raise для создания ошибок" },
                    { keyword: "Exception", points: 20, error: "Обрабатывай общие исключения" }
                ],
                farmUpdate: {
                    action: "error_system",
                    reliability: 95,
                    safetyLevel: "high",
                    visualEffect: "safety_check",
                    resources: { reliability: 50, safety: 100 }
                }
            },
            {
                id: "lesson_12",
                title: "Модули - Погодный API",
                description: "Научись использовать внешние модули и API",
                level: 6,
                experience: 650,
                coins: 325,
                theory: `
                    <h2>📦 Модули в Python</h2>
                    <p>Модули - это файлы с Python кодом, которые можно импортировать и использовать.</p>
                    
                    <pre><code># Импорт стандартных модулей
import math
import random
from datetime import datetime

# Использование модулей
print(f"Квадратный корень из 16: {math.sqrt(16)}")
print(f"Случайное число: {random.randint(1, 100)}")
print(f"Текущее время: {datetime.now()}")</code></pre>
                    
                    <h3>🎯 Зачем нужны модули?</h3>
                    <ul>
                        <li><strong>Повторное использование</strong> - не изобретай велосипед</li>
                        <li><strong>Организация</strong> - разбивай большие проекты на модули</li>
                        <li><strong>Сообщество</strong> - тысячи готовых модулей</li>
                        <li><strong>Стандартизация</strong> - единые подходы к решению задач</li>
                    </ul>
                    
                    <h3>🌤️ Пример для фермы</h3>
                    <p>Можно использовать модули для работы с погодой, базами данных, графикой и т.д.</p>
                `,
                task: "Создай систему мониторинга погоды с использованием модулей и имитацией API.",
                exampleCode: `# Система мониторинга погоды для фермы
import random
from datetime import datetime

class WeatherStation:
    def __init__(self, location="Москва"):
        self.location = location
        self.last_update = None
    
    def get_weather(self):
        """Имитация получения данных о погоде"""
        # Имитируем разные погодные условия
        conditions = ["солнечно", "дождь", "облачно", "туман", "гроза"]
        temperature = random.randint(5, 35)  # Температура от 5 до 35
        humidity = random.randint(30, 90)    # Влажность 30-90%
        
        weather_data = {
            "location": self.location,
            "temperature": temperature,
            "conditions": random.choice(conditions),
            "humidity": humidity,
            "wind_speed": random.randint(0, 15),
            "timestamp": datetime.now().strftime("%H:%M")
        }
        
        self.last_update = datetime.now()
        return weather_data
    
    def suggest_actions(self, weather):
        """Предлагает действия на основе погоды"""
        suggestions = []
        
        if weather["conditions"] == "солнечно" and weather["temperature"] > 20:
            suggestions.append("🌞 Идеальный день для посадки!")
            suggestions.append("💧 Вечерний полив растений")
            suggestions.append("☀️ Сушите сено")
        
        elif weather["conditions"] == "дождь":
            suggestions.append("🌧️ Отмените полив - идет дождь")
            suggestions.append("🏠 Работайте в теплице")
            suggestions.append("🛠️ Обслуживайте технику")
        
        elif weather["temperature"] < 10:
            suggestions.append("❄️ Включите обогрев теплицы")
            suggestions.append("🌱 Перенесите посадку на завтра")
            suggestions.append("🧥 Оденьтесь теплее")
        
        else:
            suggestions.append("☁️ Стандартный режим работ")
        
        return suggestions

# Использование системы
print("=== СИСТЕМА МОНИТОРИНГА ПОГОДЫ ===")

station = WeatherStation("Солнечная долина")
current_weather = station.get_weather()

print(f"📍 Местоположение: {current_weather['location']}")
print(f"🌡️ Температура: {current_weather['temperature']}°C")
print(f"☁️ Погода: {current_weather['conditions']}")
print(f"💧 Влажность: {current_weather['humidity']}%")
print(f"💨 Ветер: {current_weather['wind_speed']} м/с")

print("\n🤖 РЕКОМЕНДАЦИИ:")
suggestions = station.suggest_actions(current_weather)
for i, suggestion in enumerate(suggestions, 1):
    print(f"{i}. {suggestion}")`,
                initialCode: `# Создай систему мониторинга погоды
# 1. Импортируй нужные модули
# 2. Создай класс WeatherStation
# 3. Реализуй получение данных о погоде
# 4. Добавь рекомендации для фермера`,
                checks: [
                    { keyword: "import ", points: 25, error: "Используй import для импорта модулей" },
                    { keyword: "class ", points: 30, error: "Создай класс WeatherStation" },
                    { keyword: "def get_weather", points: 25, error: "Создай метод get_weather" },
                    { keyword: "def suggest_actions", points: 20, error: "Создай метод suggest_actions" }
                ],
                farmUpdate: {
                    action: "weather_system",
                    weatherMonitoring: true,
                    forecastAccuracy: 85,
                    visualEffect: "weather_display",
                    resources: { technology: 100, efficiency: 75 }
                }
            },
            {
                id: "lesson_13",
                title: "Работа с файлами - Сохранение прогресса",
                description: "Научись сохранять и загружать данные из файлов",
                level: 6,
                experience: 700,
                coins: 350,
                theory: `
                    <h2>💾 Работа с файлами</h2>
                    <p>Python позволяет читать и записывать данные в файлы.</p>
                    
                    <pre><code># Запись в файл
with open("data.txt", "w") as file:
    file.write("Привет, мир!\\n")
    file.write("Это данные моей фермы\\n")

# Чтение из файла
with open("data.txt", "r") as file:
    content = file.read()
    print(content)

# Добавление в файл
with open("data.txt", "a") as file:
    file.write("Новая запись\\n")</code></pre>
                    
                    <h3>📁 Режимы открытия файлов</h3>
                    <ul>
                        <li><strong>"r"</strong> - чтение (файл должен существовать)</li>
                        <li><strong>"w"</strong> - запись (создает или перезаписывает)</li>
                        <li><strong>"a"</strong> - добавление (добавляет в конец)</li>
                        <li><strong>"x"</strong> - создание (ошибка если файл существует)</li>
                        <li><strong>"b"</strong> - бинарный режим (для изображений и т.д.)</li>
                    </ul>
                    
                    <h3>🎯 Менеджер контекста with</h3>
                    <p>Ключевое слово with автоматически закрывает файл после использования.</p>
                `,
                task: "Создай систему сохранения прогресса фермы в JSON файл.",
                exampleCode: `# Система сохранения прогресса фермы
import json
from datetime import datetime

class FarmSaveSystem:
    def __init__(self, filename="farm_save.json"):
        self.filename = filename
    
    def save_farm(self, farm_data):
        """Сохраняет данные фермы в файл"""
        try:
            # Добавляем метаданные
            save_data = {
                "farm": farm_data,
                "metadata": {
                    "saved_at": datetime.now().isoformat(),
                    "version": "1.0",
                    "game": "CodeFarm"
                }
            }
            
            # Записываем в файл
            with open(self.filename, "w", encoding="utf-8") as file:
                json.dump(save_data, file, indent=2, ensure_ascii=False)
            
            print(f"💾 Ферма сохранена в {self.filename}")
            return True
            
        except Exception as e:
            print(f"❌ Ошибка сохранения: {e}")
            return False
    
    def load_farm(self):
        """Загружает данные фермы из файла"""
        try:
            with open(self.filename, "r", encoding="utf-8") as file:
                save_data = json.load(file)
            
            print(f"📂 Ферма загружена из {self.filename}")
            print(f"🕐 Сохранено: {save_data['metadata']['saved_at']}")
            
            return save_data["farm"]
            
        except FileNotFoundError:
            print("⚠️ Файл сохранения не найден. Создаю новую ферму...")
            return self.create_new_farm()
        except json.JSONDecodeError:
            print("❌ Ошибка чтения файла. Создаю новую ферму...")
            return self.create_new_farm()
        except Exception as e:
            print(f"❌ Неизвестная ошибка: {e}")
            return self.create_new_farm()
    
    def create_new_farm(self):
        """Создает данные для новой фермы"""
        return {
            "name": "Новая ферма",
            "level": 1,
            "resources": {
                "coins": 100,
                "water": 100,
                "seeds": 50,
                "wood": 100,
                "stone": 50
            },
            "buildings": [],
            "crops": [],
            "created_at": datetime.now().isoformat()
        }
    
    def backup_save(self):
        """Создает резервную копию сохранения"""
        import shutil
        import os
        
        if os.path.exists(self.filename):
            backup_name = f"backup_{datetime.now().strftime('%Y%m%d_%H%M%S')}.json"
            shutil.copy2(self.filename, backup_name)
            print(f"💾 Резервная копия создана: {backup_name}")
            return True
        return False

# Пример использования
print("=== СИСТЕМА СОХРАНЕНИЯ ФЕРМЫ ===")

# Создаем систему сохранения
save_system = FarmSaveSystem()

# Пример данных фермы
my_farm = {
    "name": "Солнечная долина",
    "level": 5,
    "resources": {
        "coins": 1250,
        "water": 180,
        "seeds": 30,
        "wood": 75,
        "stone": 40
    },
    "buildings": [
        {"type": "house", "level": 2},
        {"type": "barn", "level": 1}
    ],
    "crops": [
        {"type": "wheat", "growth": 75},
        {"type": "carrot", "growth": 30}
    ]
}

# Сохраняем ферму
save_system.save_farm(my_farm)

# Создаем бэкап
save_system.backup_save()

# Загружаем ферму
loaded_farm = save_system.load_farm()
print(f"\n🌾 Загруженная ферма: {loaded_farm['name']}")
print(f"💰 Монеты: {loaded_farm['resources']['coins']}")
print(f"🏗️ Построек: {len(loaded_farm['buildings'])}")`,
                initialCode: `# Создай систему сохранения прогресса
# 1. Импортируй модули json и datetime
# 2. Создай класс FarmSaveSystem
# 3. Реализуй методы save_farm и load_farm
# 4. Добавь создание резервных копий`,
                checks: [
                    { keyword: "import json", points: 25, error: "Импортируй модуль json" },
                    { keyword: "with open", points: 30, error: "Используй with open для работы с файлами" },
                    { keyword: "json.dump", points: 25, error: "Используй json.dump для сохранения JSON" },
                    { keyword: "json.load", points: 20, error: "Используй json.load для загрузки JSON" }
                ],
                farmUpdate: {
                    action: "save_system",
                    autoSave: true,
                    backupCreated: true,
                    visualEffect: "saving_animation",
                    resources: { security: 100, data_integrity: 100 }
                }
            },
            {
                id: "lesson_14",
                title: "Классы - Умная теплица",
                description: "Научись создавать классы для сложных объектов фермы",
                level: 7,
                experience: 750,
                coins: 375,
                theory: `
                    <h2>🏗️ Объектно-ориентированное программирование</h2>
                    <p>Классы позволяют создавать собственные типы данных с атрибутами и методами.</p>
                    
                    <pre><code># Создание класса
class Animal:
    # Конструктор - инициализирует объект
    def __init__(self, name, species, age):
        self.name = name
        self.species = species
        self.age = age
        self.health = 100
    
    # Метод - действие, которое может выполнять объект
    def feed(self):
        self.health += 10
        print(f"{self.name} покормлен!")
    
    def get_info(self):
        return f"{self.name} ({self.species}), {self.age} лет"

# Создание объектов (экземпляров класса)
cow = Animal("Мурка", "корова", 3)
chicken = Animal("Цыпа", "курица", 1)

# Использование методов
cow.feed()
print(chicken.get_info())</code></pre>
                    
                    <h3>🎯 Основные понятия ООП</h3>
                    <ul>
                        <li><strong>Класс</strong> - чертеж для создания объектов</li>
                        <li><strong>Объект</strong> - экземпляр класса</li>
                        <li><strong>Атрибут</strong> - данные объекта (переменные)</li>
                        <li><strong>Метод</strong> - функции объекта</li>
                        <li><strong>Конструктор</strong> - метод __init__()</li>
                    </ul>
                `,
                task: "Создай класс SmartGreenhouse для управления умной теплицей.",
                exampleCode: `# Класс умной теплицы
class SmartGreenhouse:
    """Класс для управления умной теплицей"""
    
    def __init__(self, name, area):
        # Атрибуты теплицы
        self.name = name
        self.area = area  # площадь в м²
        self.temperature = 20  # текущая температура
        self.target_temp = 25  # целевая температура
        self.humidity = 60     # влажность %
        self.light_level = 70  # уровень освещенности %
        self.plants = []       # растения в теплице
        self.is_active = False # активна ли теплица
    
    def activate(self):
        """Активирует теплицу"""
        self.is_active = True
        print(f"🏭 Теплица '{self.name}' активирована!")
        self.check_systems()
    
    def deactivate(self):
        """Деактивирует теплицу"""
        self.is_active = False
        print(f"💤 Теплица '{self.name}' переходит в спящий режим")
    
    def set_parameters(self, temp, humidity, light):
        """Устанавливает параметры среды"""
        if self.is_active:
            self.target_temp = temp
            self.humidity = humidity
            self.light_level = light
            print(f"⚙️ Параметры установлены: {temp}°C, {humidity}%, {light}%")
            self.adjust_environment()
        else:
            print("❌ Сначала активируйте теплицу!")
    
    def add_plant(self, plant_type, quantity=1):
        """Добавляет растения в теплицу"""
        for i in range(quantity):
            plant_id = f"{plant_type}_{len(self.plants) + 1}"
            self.plants.append({
                "id": plant_id,
                "type": plant_type,
                "growth": 0,
                "health": 100
            })
            print(f"🌱 Добавлено растение: {plant_type} (#{plant_id})")
    
    def check_systems(self):
        """Проверяет все системы теплицы"""
        print(f"\n🔍 ДИАГНОСТИКА ТЕПЛИЦЫ '{self.name}':")
        print(f"   🌡️ Температура: {self.temperature}°C (цель: {self.target_temp}°C)")
        print(f"   💧 Влажность: {self.humidity}%")
        print(f"   💡 Освещенность: {self.light_level}%")
        print(f"   🌿 Растений: {len(self.plants)}")
        print(f"   ⚡ Статус: {'АКТИВНА' if self.is_active else 'НЕАКТИВНА'}")
    
    def adjust_environment(self):
        """Автоматически регулирует среду"""
        if self.temperature < self.target_temp - 2:
            print("   🔥 Включаю обогрев...")
            self.temperature += 3
        elif self.temperature > self.target_temp + 2:
            print("   💨 Включаю вентиляцию...")
            self.temperature -= 3
        
        if self.humidity < 50:
            print("   💦 Увеличиваю влажность...")
            self.humidity += 10
        elif self.humidity > 80:
            print("   🌪️ Уменьшаю влажность...")
            self.humidity -= 10
    
    def grow_plants(self):
        """Ускоряет рост растений"""
        if self.is_active:
            for plant in self.plants:
                if plant["health"] > 0:
                    growth_boost = self.calculate_growth_boost()
                    plant["growth"] = min(100, plant["growth"] + growth_boost)
                    print(f"   📈 {plant['id']}: рост +{growth_boost}%")
    
    def calculate_growth_boost(self):
        """Рассчитывает ускорение роста на основе условий"""
        base_growth = 5
        temp_boost = max(0, 10 - abs(self.temperature - self.target_temp)) / 2
        humidity_boost = 2 if 50 <= self.humidity <= 70 else 0
        light_boost = self.light_level / 20
        
        return base_growth + temp_boost + humidity_boost + light_boost
    
    def get_report(self):
        """Возвращает отчет по теплице"""
        report = f"""
🏭 ОТЧЕТ ПО ТЕПЛИЦЕ '{self.name}'
📏 Площадь: {self.area} м²
⚡ Статус: {'✅ АКТИВНА' if self.is_active else '❌ НЕАКТИВНА'}
📊 Параметры:
   • Температура: {self.temperature}°C
   • Влажность: {self.humidity}%
   • Освещенность: {self.light_level}%
🌿 Растения: {len(self.plants)} шт.
"""
        
        if self.plants:
            report += "\n📈 Состояние растений:\n"
            for plant in self.plants:
                status = "✅" if plant["health"] > 70 else "⚠️" if plant["health"] > 30 else "❌"
                report += f"   {status} {plant['id']}: рост {plant['growth']}%, здоровье {plant['health']}%\n"
        
        return report

# Пример использования
print("=== СИСТЕМА УПРАВЛЕНИЯ ТЕПЛИЦАМИ ===")

# Создаем теплицы
greenhouse1 = SmartGreenhouse("Северная теплица", 50)
greenhouse2 = SmartGreenhouse("Южная теплица", 30)

# Работаем с первой теплицей
greenhouse1.activate()
greenhouse1.set_parameters(24, 65, 80)
greenhouse1.add_plant("помидоры", 3)
greenhouse1.add_plant("огурцы", 2)
greenhouse1.grow_plants()
print(greenhouse1.get_report())

print("\n" + "="*40 + "\n")

# Работаем со второй теплицей
greenhouse2.activate()
greenhouse2.set_parameters(22, 70, 75)
greenhouse2.add_plant("клубника", 5)
greenhouse2.add_plant("салат", 3)
greenhouse2.grow_plants()
print(greenhouse2.get_report())`,
                initialCode: `# Создай класс умной теплицы
# 1. Создай класс SmartGreenhouse
# 2. Добавь конструктор __init__
# 3. Реализуй методы управления
# 4. Добавь автоматическую регулировку среды`,
                checks: [
                    { keyword: "class ", points: 30, error: "Создай класс с помощью class" },
                    { keyword: "def __init__", points: 25, error: "Добавь конструктор __init__" },
                    { keyword: "self.", points: 25, error: "Используй self для доступа к атрибутам" },
                    { keyword: "def ", points: 20, error: "Добавь методы в класс" }
                ],
                farmUpdate: {
                    action: "smart_greenhouse",
                    greenhouseBuilt: true,
                    automationLevel: 80,
                    visualEffect: "greenhouse_lights",
                    resources: { glass: -100, electronics: -200, efficiency: 150 }
                }
            },
            {
                id: "lesson_15",
                title: "Наследование - Специализированные боты",
                description: "Научись создавать иерархии классов через наследование",
                level: 7,
                experience: 800,
                coins: 400,
                theory: `
                    <h2>👨‍👦 Наследование классов</h2>
                    <p>Наследование позволяет создавать новые классы на основе существующих.</p>
                    
                    <pre><code># Базовый класс
class Animal:
    def __init__(self, name):
        self.name = name
        self.energy = 100
    
    def eat(self):
        self.energy += 20
        print(f"{self.name} поел")
    
    def make_sound(self):
        print("Животное издает звук")

# Наследование
class Cow(Animal):  # Cow наследует от Animal
    def make_sound(self):
        print(f"{self.name} говорит: Мууу!")
    
    def give_milk(self):
        if self.energy > 50:
            self.energy -= 30
            print(f"{self.name} дала молоко")
            return "молоко"
        else:
            print(f"{self.name} устала")
            return None

class Chicken(Animal):
    def make_sound(self):
        print(f"{self.name} говорит: Ко-ко-ко!")
    
    def lay_egg(self):
        if self.energy > 40:
            self.energy -= 20
            print(f"{self.name} снесла яйцо")
            return "яйцо"
        return None

# Использование
cow = Cow("Буренка")
chicken = Chicken("Цыпа")

cow.make_sound()    # Мууу!
chicken.make_sound() # Ко-ко-ко!
cow.eat()           # Унаследованный метод</code></pre>
                    
                    <h3>🎯 Ключевые концепции</h3>
                    <ul>
                        <li><strong>Родительский класс</strong> - базовый класс</li>
                        <li><strong>Дочерний класс</strong> - наследует от родителя</li>
                        <li><strong>Переопределение</strong> - изменение унаследованных методов</li>
                        <li><strong>super()</strong> - обращение к родительскому классу</li>
                    </ul>
                `,
                task: "Создай иерархию классов для специализированных ботов-помощников на ферме.",
                exampleCode: `# Иерархия классов для ботов-помощников

class BaseBot:
    """Базовый класс для всех ботов на ферме"""
    
    def __init__(self, name, battery_level=100):
        self.name = name
        self.battery = battery_level
        self.is_active = False
        self.tasks_completed = 0
    
    def activate(self):
        """Активирует бота"""
        if self.battery > 20:
            self.is_active = True
            print(f"🤖 {self.name} активирован!")
            return True
        else:
            print(f"❌ {self.name}: недостаточно заряда ({self.battery}%)")
            return False
    
    def deactivate(self):
        """Деактивирует бота"""
        self.is_active = False
        print(f"💤 {self.name} переходит в спящий режим")
    
    def check_battery(self):
        """Показывает уровень заряда"""
        print(f"🔋 {self.name}: заряд {self.battery}%")
        return self.battery
    
    def charge(self, amount):
        """Заряжает бота"""
        self.battery = min(100, self.battery + amount)
        print(f"⚡ {self.name} заряжен на {amount}%")
        return self.battery
    
    def perform_task(self, task_name):
        """Выполняет задачу (базовый метод)"""
        if self.is_active and self.battery > 10:
            self.battery -= 10
            self.tasks_completed += 1
            print(f"⚡ {self.name} выполняет: {task_name}")
            return True
        else:
            print(f"❌ {self.name} не может выполнить задачу")
            return False
    
    def get_status(self):
        """Возвращает статус бота"""
        return {
            "name": self.name,
            "battery": self.battery,
            "active": self.is_active,
            "tasks": self.tasks_completed
        }

class GardenerBot(BaseBot):
    """Специализированный бот для работы в саду"""
    
    def __init__(self, name, battery_level=100, tools=None):
        # Вызов конструктора родительского класса
        super().__init__(name, battery_level)
        
        # Специфичные для GardenerBot атрибуты
        self.tools = tools or ["лейка", "секатор", "грабли"]
        self.plants_watered = 0
        self.plants_pruned = 0
        self.gardening_experience = 0
    
    def water_plants(self, plant_count):
        """Поливает растения"""
        if self.perform_task(f"полив {plant_count} растений"):
            print(f"💧 Поливаю {plant_count} растений...")
            self.plants_watered += plant_count
            self.gardening_experience += plant_count * 2
            print(f"📈 Опыт садоводства: +{plant_count * 2}")
            return True
        return False
    
    def prune_trees(self, tree_count):
        """Обрезает деревья"""
        if self.perform_task(f"обрезка {tree_count} деревьев"):
            print(f"✂️ Обрезаю {tree_count} деревьев...")
            self.plants_pruned += tree_count
            self.gardening_experience += tree_count * 5
            print(f"📈 Опыт садоводства: +{tree_count * 5}")
            return True
        return False
    
    def plant_seeds(self, seed_type, count):
        """Сажает семена"""
        if self.perform_task(f"посадка {count} {seed_type}"):
            print(f"🌱 Сажаю {count} семян {seed_type}...")
            self.gardening_experience += count * 3
            print(f"📈 Опыт садоводства: +{count * 3}")
            return True
        return False
    
    def get_gardener_stats(self):
        """Возвращает статистику садовода"""
        base_stats = self.get_status()
        base_stats.update({
            "plants_watered": self.plants_watered,
            "plants_pruned": self.plants_pruned,
            "gardening_experience": self.gardening_experience,
            "tools": self.tools
        })
        return base_stats

class BuilderBot(BaseBot):
    """Специализированный бот для строительных работ"""
    
    def __init__(self, name, battery_level=100, strength=100):
        super().__init__(name, battery_level)
        self.strength = strength
        self.buildings_constructed = []
        self.materials_used = {}
    
    def build_structure(self, structure_type, materials):
        """Строит структуру"""
        task_name = f"строительство {structure_type}"
        if self.perform_task(task_name):
            print(f"🏗️ Строю {structure_type}...")
            self.buildings_constructed.append(structure_type)
            
            # Учитываем использованные материалы
            for material, amount in materials.items():
                if material in self.materials_used:
                    self.materials_used[material] += amount
                else:
                    self.materials_used[material] = amount
            
            print(f"📦 Построено: {len(self.buildings_constructed)} объектов")
            return True
        return False
    
    def move_material(self, material, weight, distance):
        """Перемещает материалы"""
        if weight > self.strength:
            print(f"❌ {material} слишком тяжелый! Максимум: {self.strength}кг")
            return False
        
        task_name = f"перенос {material} ({weight}кг)"
        if self.perform_task(task_name):
            print(f"📦 Переношу {material} ({weight}кг) на {distance}м...")
            return True
        return False
    
    def repair_structure(self, structure_name):
        """Ремонтирует структуру"""
        if self.perform_task(f"ремонт {structure_name}"):
            print(f"🔧 Ремонтирую {structure_name}...")
            print(f"✅ {structure_name} отремонтирован!")
            return True
        return False
    
    def get_builder_stats(self):
        """Возвращает статистику строителя"""
        base_stats = self.get_status()
        base_stats.update({
            "buildings": self.buildings_constructed,
            "materials_used": self.materials_used,
            "strength": self.strength
        })
        return base_stats

class HarvesterBot(BaseBot):
    """Специализированный бот для сбора урожая"""
    
    def __init__(self, name, battery_level=100, speed=1.0):
        super().__init__(name, battery_level)
        self.speed = speed  # множитель скорости
        self.crops_harvested = {}
        self.total_weight = 0
    
    def harvest_crop(self, crop_type, area_size):
        """Собирает урожай"""
        # Расчет количества урожая на основе площади
        estimated_yield = area_size * 10  # 10 кг на единицу площади
        
        task_name = f"сбор {crop_type}"
        if self.perform_task(task_name):
            print(f"📦 Собираю {crop_type} с площади {area_size}...")
            
            # Обновляем статистику
            if crop_type in self.crops_harvested:
                self.crops_harvested[crop_type] += estimated_yield
            else:
                self.crops_harvested[crop_type] = estimated_yield
            
            self.total_weight += estimated_yield
            
            print(f"✅ Собрано ~{estimated_yield}кг {crop_type}")
            print(f"📊 Всего собрано: {self.total_weight}кг")
            
            return estimated_yield
        return 0
    
    def sort_produce(self, crop_type):
        """Сортирует собранный урожай"""
        if self.perform_task(f"сортировка {crop_type}"):
            print(f"🔍 Сортирую {crop_type}...")
            
            # Имитация сортировки по качеству
            qualities = ["высший сорт", "первый сорт", "второй сорт"]
            sorted_result = {}
            
            for quality in qualities:
                amount = len(qualities) * 10  # Примерное количество
                sorted_result[quality] = amount
                print(f"   • {quality}: {amount}кг")
            
            return sorted_result
        return {}
    
    def transport_to_storage(self, crop_type, weight):
        """Транспортирует урожай на склад"""
        if self.perform_task(f"транспортировка {crop_type}"):
            print(f"🚚 Везу {weight}кг {crop_type} на склад...")
            print(f"✅ {crop_type} доставлен на склад!")
            return True
        return False
    
    def get_harvester_stats(self):
        """Возвращает статистику сборщика"""
        base_stats = self.get_status()
        base_stats.update({
            "crops_harvested": self.crops_harvested,
            "total_weight": self.total_weight,
            "speed": self.speed
        })
        return base_stats

# Демонстрация работы системы
print("=== СИСТЕМА СПЕЦИАЛИЗИРОВАННЫХ БОТОВ ===")

# Создаем ботов разных типов
gardener = GardenerBot("Зеленый Палец", 85, ["умная лейка", "робо-секатор"])
builder = BuilderBot("Железный Строитель", 90, 150)
harvester = HarvesterBot("Урожайный Робот", 80, 1.5)

print("\n🌿 РАБОТА САДОВОДА:")
gardener.activate()
gardener.check_battery()
gardener.water_plants(8)
gardener.prune_trees(3)
gardener.plant_seeds("морковь", 12)
print("📊 Статистика садовода:", gardener.get_gardener_stats())

print("\n🏗️ РАБОТА СТРОИТЕЛЯ:")
builder.activate()
builder.check_battery()
builder.build_structure("сарай", {"дерево": 50, "гвозди": 200})
builder.build_structure("теплица", {"стекло": 30, "металл": 40})
builder.move_material("дерево", 100, 50)
builder.move_material("кирпичи", 200, 10)  # Слишком тяжело
print("📊 Статистика строителя:", builder.get_builder_stats())

print("\n📦 РАБОТА СБОРЩИКА:")
harvester.activate()
harvester.check_battery()
harvester.harvest_crop("пшеница", 5)
harvester.harvest_crop("картофель", 3)
harvester.sort_produce("пшеница")
harvester.transport_to_storage("картофель", 30)
print("📊 Статистика сборщика:", harvester.get_harvester_stats())

print(f"\n🎯 ИТОГИ:")
print(f"Садовод '{gardener.name}': опыт {gardener.gardening_experience}")
print(f"Строитель '{builder.name}': построил {builder.buildings_constructed}")
print(f"Сборщик '{harvester.name}': собрал {harvester.total_weight}кг урожая")`,
                initialCode: `# Создай иерархию классов для ботов
# 1. Создай базовый класс BaseBot
# 2. Создай специализированные классы через наследование
# 3. Добавь уникальные методы для каждого типа ботов
# 4. Протестируй работу системы`,
                checks: [
                    { keyword: "class BaseBot", points: 20, error: "Создай базовый класс BaseBot" },
                    { keyword: "class GardenerBot(BaseBot)", points: 20, error: "Создай дочерний класс GardenerBot" },
                    { keyword: "super().__init__", points: 20, error: "Используй super() для вызова родительского конструктора" },
                    { keyword: "def water_plants", points: 20, error: "Добавь специализированный метод" },
                    { keyword: "def build_structure", points: 20, error: "Добавь метод для другого типа бота" }
                ],
                farmUpdate: {
                    action: "specialized_bots",
                    botsDeployed: 3,
                    automationLevel: 200,
                    visualEffect: "bots_working",
                    resources: { robots: 3, efficiency: 250, technology: 300 }
                }
            },

            // УРОКИ 16-20: ПРОДВИНУТЫЕ КОНЦЕПЦИИ
            {
                id: "lesson_16",
                title: "Исключения - Кастомные ошибки фермы",
                description: "Научись создавать собственные типы исключений для фермы",
                level: 8,
                experience: 850,
                coins: 425,
                theory: `
                    <h2>🚨 Собственные исключения</h2>
                    <p>Вы можете создавать собственные типы исключений для специфичных ошибок вашего приложения.</p>
                    
                    <pre><code># Создание кастомного исключения
class FarmError(Exception):
    """Базовое исключение для всех ошибок на ферме"""
    def __init__(self, message, error_code):
        super().__init__(message)
        self.error_code = error_code
        self.timestamp = datetime.now()
    
    def get_details(self):
        return f"Ошибка {self.error_code}: {self.args[0]}"

# Специализированные исключения
class WaterError(FarmError):
    pass

class SoilError(FarmError):
    pass

# Использование
def check_water_level(level):
    if level < 20:
        raise WaterError("Недостаточно воды!", "WATER_001")

try:
    check_water_level(10)
except WaterError as e:
    print(f"Водная ошибка: {e.get_details()}")</code></pre>
                `,
                task: "Создай иерархию кастомных исключений для различных ошибок на ферме.",
                exampleCode: `# Кастомные исключения для фермы
from datetime import datetime

# Базовые исключения
class FarmException(Exception):
    """Базовое исключение для всех ошибок фермы"""
    def __init__(self, message, error_code):
        super().__init__(message)
        self.error_code = error_code
        self.timestamp = datetime.now()
    
    def log_error(self):
        """Логирует ошибку"""
        return f"[{self.timestamp}] {self.error_code}: {self.args[0]}"

# Специализированные исключения
class ResourceException(FarmException):
    """Исключения связанные с ресурсами"""
    pass

class EquipmentException(FarmException):
    """Исключения связанные с оборудованием"""
    pass

class PlantException(FarmException):
    """Исключения связанные с растениями"""
    pass

# Конкретные типы ошибок
class NotEnoughWater(ResourceException):
    def __init__(self, current_level, required_level):
        message = f"Недостаточно воды! Есть: {current_level}л, нужно: {required_level}л"
        super().__init__(message, "RESOURCE_WATER_001")

class PoorSoilQuality(PlantException):
    def __init__(self, plant_type, ph_level, optimal_ph):
        message = f"Почва не подходит для {plant_type}! pH: {ph_level}, оптимальный: {optimal_ph}"
        super().__init__(message, "PLANT_SOIL_001")

class BrokenEquipment(EquipmentException):
    def __init__(self, equipment_name, problem):
        message = f"{equipment_name} сломан! Проблема: {problem}"
        super().__init__(message, "EQUIPMENT_001")

class NoSeedsAvailable(ResourceException):
    def __init__(self, seed_type):
        message = f"Нет семян {seed_type} в наличии!"
        super().__init__(message, "RESOURCE_SEEDS_001")

# Класс для управления фермой с обработкой ошибок
class FarmManager:
    def __init__(self):
        self.water_tank = 500  # литров
        self.soil_ph = 6.5
        self.equipment_status = {
            "трактор": "работает",
            "косилка": "сломан",
            "система_полива": "работает"
        }
        self.seeds_inventory = {
            "пшеница": 50,
            "морковь": 30,
            "картофель": 20
        }
    
    def water_field(self, area_size):
        """Поливает поле с обработкой ошибок"""
        water_needed = area_size * 10  # 10л на м²
        
        try:
            # Проверяем наличие воды
            if self.water_tank < water_needed:
                raise NotEnoughWater(self.water_tank, water_needed)
            
            # Проверяем систему полива
            if self.equipment_status["система_полива"] != "работает":
                raise BrokenEquipment("система полива", "нет давления")
            
            # Выполняем полив
            print(f"💧 Поливаю поле {area_size}м²...")
            self.water_tank -= water_needed
            print(f"✅ Полив завершен! Осталось воды: {self.water_tank}л")
            
            return water_needed
            
        except NotEnoughWater as e:
            print(f"🚨 {e.log_error()}")
            print("💡 Решение: наполните бак водой")
            return 0
        
        except BrokenEquipment as e:
            print(f"🚨 {e.log_error()}")
            print("💡 Решение: почините систему полива")
            return 0
        
        except Exception as e:
            print(f"⚠️ Неизвестная ошибка: {e}")
            return 0
    
    def plant_crop(self, crop_type, area_size):
        """Сажает культуру с обработкой ошибок"""
        seeds_needed = area_size * 2  # 2 семени на м²
        
        try:
            # Проверяем наличие семян
            if crop_type not in self.seeds_inventory:
                raise NoSeedsAvailable(crop_type)
            
            if self.seeds_inventory[crop_type] < seeds_needed:
                raise NoSeedsAvailable(crop_type)
            
            # Проверяем качество почвы
            optimal_ph = {
                "пшеница": 6.0,
                "морковь": 6.5,
                "картофель": 5.5
            }
            
            if crop_type in optimal_ph:
                if abs(self.soil_ph - optimal_ph[crop_type]) > 1:
                    raise PoorSoilQuality(crop_type, self.soil_ph, optimal_ph[crop_type])
            
            # Сажаем культуру
            print(f"🌱 Сажаю {crop_type} на площади {area_size}м²...")
            self.seeds_inventory[crop_type] -= seeds_needed
            print(f"✅ {crop_type} посажена! Осталось семян: {self.seeds_inventory[crop_type]}")
            
            return seeds_needed
            
        except (NoSeedsAvailable, PoorSoilQuality) as e:
            print(f"🚨 {e.log_error()}")
            return 0
        
        except Exception as e:
            print(f"⚠️ Неизвестная ошибка: {e}")
            return 0
    
    def use_equipment(self, equipment_name):
        """Использует оборудование с обработкой ошибок"""
        try:
            if equipment_name not in self.equipment_status:
                raise EquipmentException(f"Оборудование '{equipment_name}' не найдено", "EQUIPMENT_404")
            
            if self.equipment_status[equipment_name] != "работает":
                raise BrokenEquipment(equipment_name, self.equipment_status[equipment_name])
            
            print(f"🚜 Использую {equipment_name}...")
            print(f"✅ {equipment_name} работает отлично!")
            
            return True
            
        except BrokenEquipment as e:
            print(f"🚨 {e.log_error()}")
            print("💡 Решение: вызовите механика для ремонта")
            return False
        
        except EquipmentException as e:
            print(f"🚨 {e.log_error()}")
            return False
        
        except Exception as e:
            print(f"⚠️ Неизвестная ошибка: {e}")
            return False

# Демонстрация системы
print("=== СИСТЕМА ОБРАБОТКИ ОШИБОК ФЕРМЫ ===")

manager = FarmManager()

print("\n1. Попытка полива большого поля:")
manager.water_field(100)  # Требуется 1000л, есть 500л

print("\n2. Попытка посадки без семян:")
manager.plant_crop("томаты", 10)  # Нет томатов в инвентаре

print("\n3. Попытка использования сломанной техники:")
manager.use_equipment("косилка")

print("\n4. Попытка посадки в неподходящую почву:")
# Меняем pH почвы для демонстрации
manager.soil_ph = 4.0  # Очень кислая почва
manager.plant_crop("пшеница", 5)  # Пшеница предпочитает pH ~6.0

print("\n5. Успешные операции:")
manager.soil_ph = 6.5  # Возвращаем нормальный pH
manager.water_tank = 1000  # Добавляем воды
manager.equipment_status["косилка"] = "работает"  # Чиним косилку

manager.water_field(20)           # 200л - хватит
manager.plant_crop("морковь", 5)  # pH подходит
manager.use_equipment("трактор")  # Трактор работает

print("\n6. Создание отчета об ошибках:")
# Можно сохранять ошибки в лог
error_log = []
def log_error(error):
    error_log.append({
        "error": error.__class__.__name__,
        "message": str(error),
        "code": error.error_code if hasattr(error, 'error_code') else "UNKNOWN",
        "timestamp": datetime.now().isoformat()
    })

# Тестируем логирование
try:
    raise NotEnoughWater(10, 100)
except NotEnoughWater as e:
    log_error(e)

try:
    raise BrokenEquipment("комбайн", "нет топлива")
except BrokenEquipment as e:
    log_error(e)

print(f"📝 Лог ошибок ({len(error_log)} записей):")
for error in error_log:
    print(f"   • {error['error']}: {error['message']}")`,
                initialCode: `# Создай систему кастомных исключений
# 1. Создай базовый класс FarmException
# 2. Создай специализированные классы исключений
# 3. Реализуй обработку ошибок в FarmManager
# 4. Протестируй различные сценарии ошибок`,
                checks: [
                    { keyword: "class FarmException", points: 25, error: "Создай базовый класс исключений" },
                    { keyword: "class NotEnoughWater", points: 25, error: "Создай специализированный класс исключения" },
                    { keyword: "raise ", points: 25, error: "Используй raise для вызова исключений" },
                    { keyword: "except ", points: 25, error: "Используй except для обработки исключений" }
                ],
                farmUpdate: {
                    action: "error_handling_system",
                    errorLogging: true,
                    systemReliability: 95,
                    visualEffect: "error_display",
                    resources: { stability: 200, reliability: 150 }
                }
            },
            {
                id: "lesson_17",
                title: "Декораторы - Мониторинг производительности",
                description: "Научись использовать декораторы для отслеживания и оптимизации",
                level: 8,
                experience: 900,
                coins: 450,
                theory: `
                    <h2>🎨 Декораторы функций</h2>
                    <p>Декораторы позволяют изменять поведение функций без изменения их кода.</p>
                    
                    <pre><code># Простой декоратор
def timer_decorator(func):
    def wrapper(*args, **kwargs):
        import time
        start = time.time()
        result = func(*args, **kwargs)
        end = time.time()
        print(f"Функция {func.__name__} выполнилась за {end-start:.2f}с")
        return result
    return wrapper

# Использование декоратора
@timer_decorator
def slow_function():
    import time
    time.sleep(2)
    print("Функция выполнена")

slow_function()  # Будет показано время выполнения</code></pre>
                `,
                task: "Создай систему декораторов для мониторинга производительности фермы.",
                exampleCode: `# Система декораторов для мониторинга производительности фермы
import time
from datetime import datetime
from functools import wraps

# Декораторы для мониторинга
def measure_time(func):
    """Декоратор для измерения времени выполнения"""
    @wraps(func)  # Сохраняем метаданные функции
    def wrapper(*args, **kwargs):
        start_time = time.time()
        print(f"⏱️ Начинаю выполнение: {func.__name__}")
        
        result = func(*args, **kwargs)
        
        end_time = time.time()
        duration = end_time - start_time
        print(f"✅ {func.__name__} завершена за {duration:.2f} секунд")
        
        return result
    return wrapper

def log_action(action_name):
    """Декоратор для логирования действий"""
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            timestamp = datetime.now().strftime("%H:%M:%S")
            print(f"📝 [{timestamp}] Начато действие: {action_name}")
            
            result = func(*args, **kwargs)
            
            timestamp = datetime.now().strftime("%H:%M:%S")
            print(f"✅ [{timestamp}] Завершено действие: {action_name}")
            
            return result
        return wrapper
    return decorator

def check_resources(water_needed=0, energy_needed=0):
    """Декоратор для проверки ресурсов"""
    def decorator(func):
        @wraps(func)
        def wrapper(self, *args, **kwargs):
            # Проверяем наличие ресурсов
            if hasattr(self, 'resources'):
                if self.resources.get('water', 0) < water_needed:
                    print(f"❌ Недостаточно воды! Нужно: {water_needed}л, есть: {self.resources.get('water', 0)}л")
                    return False
                
                if self.resources.get('energy', 0) < energy_needed:
                    print(f"❌ Недостаточно энергии! Нужно: {energy_needed}Вт, есть: {self.resources.get('energy', 0)}Вт")
                    return False
            
            # Выполняем функцию
            result = func(self, *args, **kwargs)
            
            # Списание ресурсов
            if hasattr(self, 'resources'):
                self.resources['water'] = max(0, self.resources.get('water', 0) - water_needed)
                self.resources['energy'] = max(0, self.resources.get('energy', 0) - energy_needed)
                print(f"📉 Ресурсы списаны: вода -{water_needed}л, энергия -{energy_needed}Вт")
            
            return result
        return wrapper
    return decorator

def retry_on_failure(max_attempts=3, delay=1):
    """Декоратор для повторения при ошибках"""
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            attempts = 0
            while attempts < max_attempts:
                try:
                    return func(*args, **kwargs)
                except Exception as e:
                    attempts += 1
                    print(f"⚠️ Попытка {attempts} не удалась: {e}")
                    
                    if attempts == max_attempts:
                        print("❌ Достигнут лимит попыток!")
                        raise
                    
                    print(f"🔄 Пробую снова через {delay} секунд...")
                    time.sleep(delay)
        return wrapper
    return decorator

# Класс фермы с использованием декораторов
class MonitoredFarm:
    def __init__(self):
        self.resources = {
            'water': 1000,
            'energy': 500,
            'seeds': 100,
            'wood': 200,
            'stone': 100
        }
        self.harvest = 0
        self.performance_log = []
    
    @measure_time
    @log_action("полив поля")
    @check_resources(water_needed=200, energy_needed=50)
    def water_field(self, area):
        """Поливает поле с мониторингом"""
        print(f"💧 Поливаю поле {area}м²")
        time.sleep(area * 0.01)  # Имитация времени работы
        self.harvest += area * 0.1
        return f"Поле полито, ожидаемый урожай +{area * 0.1:.1f}кг"
    
    @measure_time
    @log_action("посадка культур")
    @check_resources(water_needed=100, energy_needed=30)
    def plant_crops(self, crops):
        """Сажает культуры с мониторингом"""
        print(f"🌱 Сажаю {len(crops)} культур: {', '.join(crops)}")
        time.sleep(len(crops) * 0.2)  # Имитация времени работы
        return f"Посажено {len(crops)} культур"
    
    @measure_time
    @log_action("сбор урожая")
    @check_resources(energy_needed=80)
    def harvest_crops(self):
        """Собирает урожай с мониторингом"""
        print(f"📦 Собираю урожай... Текущий: {self.harvest}кг")
        time.sleep(self.harvest * 0.01)
        harvested = self.harvest
        self.harvest = 0
        return f"Собрано {harvested}кг урожая"
    
    @retry_on_failure(max_attempts=2, delay=0.5)
    def connect_to_sensor(self, sensor_name):
        """Имитация подключения к датчику с возможными ошибками"""
        import random
        if random.random() < 0.6:  # 60% chance of failure
            raise ConnectionError(f"Не удалось подключиться к датчику {sensor_name}")
        print(f"✅ Успешно подключен к датчику {sensor_name}")
        return True
    
    def show_status(self):
        """Показывает текущий статус фермы"""
        print(f"\n📊 СТАТУС ФЕРМЫ:")
        print(f"💧 Вода: {self.resources['water']}л")
        print(f"⚡ Энергия: {self.resources['energy']}Вт")
        print(f"🌾 Урожай: {self.harvest}кг")
        print(f"🌱 Семена: {self.resources['seeds']}")
        print(f"🪵 Дерево: {self.resources['wood']}")
        print(f"🪨 Камень: {self.resources['stone']}")

# Демонстрация работы декораторов
print("=== СИСТЕМА МОНИТОРИНГА ПРОИЗВОДИТЕЛЬНОСТИ ===")

farm = MonitoredFarm()

print("\n=== ВЫПОЛНЕНИЕ ОПЕРАЦИЙ С МОНИТОРИНГОМ ===")
farm.show_status()

print("\n" + "="*50)
result1 = farm.water_field(100)
print(f"Результат: {result1}")

print("\n" + "="*50)
result2 = farm.plant_crops(["морковь", "картофель", "лук"])
print(f"Результат: {result2}")

print("\n" + "="*50)
farm.show_status()

print("\n" + "="*50)
print("=== ТЕСТ ПОВТОРА ПРИ ОШИБКАХ ===")
farm.connect_to_sensor("температуры")

print("\n" + "="*50)
result3 = farm.harvest_crops()
print(f"Результат: {result3}")

# Дополнительный пример: декоратор для кэширования
def cache_results(max_size=100):
    """Декоратор для кэширования результатов"""
    cache = {}
    
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            # Создаем ключ кэша на основе аргументов
            cache_key = str(args) + str(kwargs)
            
            # Проверяем наличие в кэше
            if cache_key in cache:
                print(f"⚡ Использую кэшированный результат для {func.__name__}")
                return cache[cache_key]
            
            # Выполняем функцию
            result = func(*args, **kwargs)
            
            # Сохраняем в кэш (с ограничением размера)
            if len(cache) >= max_size:
                # Удаляем самый старый элемент
                oldest_key = next(iter(cache))
                del cache[oldest_key]
            
            cache[cache_key] = result
            return result
        return wrapper
    return decorator

# Пример использования кэширующего декоратора
class WeatherService:
    @cache_results(max_size=10)
    def get_weather_forecast(self, location, days):
        """Имитация запроса к API погоды"""
        print(f"🌤️ Запрашиваю погоду для {location} на {days} дней...")
        time.sleep(1)  # Имитация задержки сети
        return {
            "location": location,
            "days": days,
            "forecast": "солнечно",
            "temperature": 25
        }

print("\n=== ТЕСТ КЭШИРОВАНИЯ ===")
weather_service = WeatherService()

# Первый запрос - будет задержка
start = time.time()
forecast1 = weather_service.get_weather_forecast("Москва", 3)
print(f"Прогноз 1: {forecast1['forecast']}, время: {time.time() - start:.2f}с")

# Второй запрос с теми же параметрами - из кэша
start = time.time()
forecast2 = weather_service.get_weather_forecast("Москва", 3)
print(f"Прогноз 2: {forecast2['forecast']}, время: {time.time() - start:.2f}с")

# Третий запрос с другими параметрами - снова задержка
start = time.time()
forecast3 = weather_service.get_weather_forecast("Санкт-Петербург", 5)
print(f"Прогноз 3: {forecast3['forecast']}, время: {time.time() - start:.2f}с")`,
                initialCode: `# Создай систему декораторов для мониторинга
# 1. Создай декораторы measure_time, log_action, check_resources
# 2. Примени их к методам класса MonitoredFarm
# 3. Реализуй декоратор retry_on_failure
# 4. Добавь кэширующий декоратор`,
                checks: [
                    { keyword: "def measure_time", points: 25, error: "Создай декоратор measure_time" },
                    { keyword: "@wraps", points: 25, error: "Используй @wraps для сохранения метаданных" },
                    { keyword: "@measure_time", points: 25, error: "Примени декоратор к методу" },
                    { keyword: "def wrapper", points: 25, error: "Создай функцию-обертку в декораторе" }
                ],
                farmUpdate: {
                    action: "performance_monitoring",
                    monitoringActive: true,
                    optimizationLevel: 85,
                    visualEffect: "performance_dashboard",
                    resources: { analytics: 150, efficiency: 200 }
                }
                           },
                {
                    id: "lesson_18",
                    title: "Генераторы - Поточная обработка урожая",
                    description: "Научись использовать генераторы для эффективной обработки данных",
                    level: 9,
                    experience: 950,
                    coins: 475,
                    theory: `
                        <h2>🌀 Генераторы в Python</h2>
                        <p>Генераторы - это функции, которые возвращают итератор и могут приостанавливать выполнение.</p>
                        
                        <pre><code># Простой генератор
def count_up_to(max):
    count = 1
    while count <= max:
        yield count  # yield вместо return
        count += 1

# Использование генератора
for num in count_up_to(5):
    print(num)  # 1, 2, 3, 4, 5

# Генераторные выражения
squares = (x*x for x in range(10))
for square in squares:
    print(square)</code></pre>
                    `,
                    task: "Создай систему поточной обработки урожая с использованием генераторов.",
                    exampleCode: `# Система поточной обработки урожая с генераторами

# 1. Базовые генераторы для фермы
def crop_generator(crop_type, quantity):
    """Генератор для последовательной обработки урожая"""
    for i in range(quantity):
        crop_id = f"{crop_type}_{i+1}"
        print(f"🌱 Обрабатываю {crop_type} #{i+1}")
        yield {
            "id": crop_id,
            "type": crop_type,
            "weight": 2.5,
            "quality": 85 + i % 15
        }

# 2. Сложные генераторы для обработки
class HarvestProcessor:
    def __init__(self):
        self.total_weight = 0
        self.processed_count = 0
    
    def stream_harvest(self, crop_type, total_quantity, batch_size=10):
        """Генератор для поточного сбора урожая"""
        print(f"🚜 Начинаю сбор {crop_type}...")
        
        for batch_num in range(0, total_quantity, batch_size):
            current_batch = min(batch_size, total_quantity - batch_num)
            batch_weight = current_batch * 2
            
            self.processed_count += current_batch
            self.total_weight += batch_weight
            
            yield {
                'batch_number': batch_num // batch_size + 1,
                'items_in_batch': current_batch,
                'batch_weight': batch_weight,
                'total_processed': self.processed_count,
                'total_weight': self.total_weight
            }
    
    def quality_filter(self, harvest_stream, min_quality=0.7):
        """Генератор с фильтрацией по качеству"""
        for crop in harvest_stream:
            import random
            quality = random.random()
            
            if quality >= min_quality:
                yield {
                    **crop,
                    'quality': quality,
                    'status': '✅ Продажа'
                }
            else:
                yield {
                    **crop,
                    'quality': quality,
                    'status': '❌ На переработку'
                }

# 3. Демонстрация работы
print("=== СИСТЕМА ПОТОЧНОЙ ОБРАБОТКИ УРОЖАЯ ===")

# Простой генератор
print("\n1. 📦 ОБРАБОТКА УРОЖАЯ МОРКОВИ:")
for морковь in crop_generator("морковь", 5):
    print(f"   🥕 {морковь['id']} - качество: {морковь['quality']}%")

# Поточный сбор большого урожая
print("\n2. 🚜 ПОТОЧНЫЙ СБОР ПШЕНИЦЫ:")
processor = HarvestProcessor()

for партия in processor.stream_harvest("пшеница", 100, 15):
    print(f"   📦 Партия {партия['batch_number']}: {партия['items_in_batch']} единиц")
    print(f"   📊 Всего: {партия['total_processed']} единиц, {партия['total_weight']}кг")

# Фильтрация по качеству
print("\n3. 🔍 ФИЛЬТРАЦИЯ ПО КАЧЕСТВУ:")
harvest_stream = processor.stream_harvest("картофель", 20, 5)
quality_stream = processor.quality_filter(harvest_stream, 0.8)

for item in quality_stream:
    print(f"   {item['status']} {item['type']}: качество {item['quality']:.2f}")

print("\n🎉 ОБРАБОТКА УРОЖАЯ ЗАВЕРШЕНА!")`,
                    initialCode: `# Создай систему обработки урожая с генераторами
# 1. Создай генератор crop_generator
# 2. Реализуй класс HarvestProcessor
# 3. Добавь фильтрацию по качеству
# 4. Протестируй систему`,
                    checks: [
                        { keyword: "def crop_generator", points: 25, error: "Создай генераторную функцию" },
                        { keyword: "yield", points: 25, error: "Используй yield для создания генератора" },
                        { keyword: "for ", points: 25, error: "Используй цикл for для обработки генератора" },
                        { keyword: "class HarvestProcessor", points: 25, error: "Создай класс для обработки урожая" }
                    ],
                    farmUpdate: {
                        action: "stream_processing",
                        processingSpeed: "fast",
                        efficiency: 120,
                        visualEffect: "conveyor_belt",
                        resources: { automation: 150, efficiency: 180 }
                    }
                },

                // УРОКИ 19-27: ПРОДОЛЖЕНИЕ
                {
                    id: "lesson_19",
                    title: "Асинхронность - Параллельные задачи",
                    description: "Научись выполнять несколько задач одновременно на ферме",
                    level: 10,
                    experience: 1000,
                    coins: 500,
                    theory: `<h2>⚡ Асинхронное программирование</h2><p>Позволяет выполнять задачи параллельно без блокировки.</p>`,
                    task: "Создай систему параллельного управления процессами на ферме.",
                    exampleCode: `# Система параллельных задач фермы
import asyncio
import random

async def water_field(field_name, area):
    print(f"💧 Начинаю полив поля {field_name}")
    await asyncio.sleep(area * 0.1)
    print(f"✅ Поле {field_name} полито")
    return f"{area}м² полито"

async def plant_crops(field_name, crops):
    print(f"🌱 Сажаю культуры на поле {field_name}")
    for crop in crops:
        await asyncio.sleep(0.5)
        print(f"   Посажена {crop}")
    return f"Посажено {len(crops)} культур"

async def harvest_field(field_name, quantity):
    print(f"📦 Начинаю сбор с поля {field_name}")
    await asyncio.sleep(quantity * 0.2)
    print(f"✅ С поля {field_name} собрано {quantity} единиц")
    return f"{quantity} единиц урожая"

async def manage_farm():
    print("=== ПАРАЛЛЕЛЬНОЕ УПРАВЛЕНИЕ ФЕРМОЙ ===")
    
    tasks = [
        water_field("Северное", 50),
        plant_crops("Восточное", ["пшеница", "ячмень"]),
        harvest_field("Южное", 30),
        water_field("Западное", 40),
        plant_crops("Центральное", ["картофель", "морковь"])
    ]
    
    results = await asyncio.gather(*tasks)
    
    print("\n📊 РЕЗУЛЬТАТЫ:")
    for result in results:
        print(f"   • {result}")

# Запуск
asyncio.run(manage_farm())`,
                    initialCode: `# Создай систему параллельных задач
# 1. Используй async/await
# 2. Создай несколько асинхронных функций
# 3. Запусти задачи параллельно с asyncio.gather`,
                    checks: [
                        { keyword: "async def", points: 30, error: "Используй async для асинхронных функций" },
                        { keyword: "await ", points: 30, error: "Используй await для ожидания" },
                        { keyword: "asyncio.gather", points: 20, error: "Используй gather для параллельного выполнения" },
                        { keyword: "asyncio.run", points: 20, error: "Используй run для запуска" }
                    ],
                    farmUpdate: {
                        action: "parallel_tasks",
                        concurrency: 5,
                        speedBoost: 300,
                        visualEffect: "multiple_tasks",
                        resources: { efficiency: 250, speed: 200 }
                    }
                },
                {
                    id: "lesson_20",
                    title: "Контекстные менеджеры - Управление ресурсами",
                    description: "Научись безопасно управлять ресурсами фермы",
                    level: 10,
                    experience: 1050,
                    coins: 525,
                    theory: `<h2>🔧 Контекстные менеджеры</h2><p>Автоматическое управление ресурсами с помощью with.</p>`,
                    task: "Создай систему безопасного управления ресурсами фермы.",
                    exampleCode: `# Контекстные менеджеры для фермы
from contextlib import contextmanager
import time

class GreenhouseManager:
    def __init__(self, name):
        self.name = name
        self.active = False
    
    def __enter__(self):
        print(f"🏭 Открываю теплицу {self.name}")
        self.active = True
        self.start_time = time.time()
        return self
    
    def __exit__(self, exc_type, exc_val, exc_tb):
        self.active = False
        duration = time.time() - self.start_time
        print(f"🔒 Закрываю теплицу {self.name}")
        print(f"⏰ Работала: {duration:.1f} секунд")
        if exc_type:
            print(f"⚠️ Была ошибка: {exc_type.__name__}")

@contextmanager
def farm_resource(resource_name, amount):
    """Контекстный менеджер для ресурсов"""
    print(f"📦 Использую {amount} единиц {resource_name}")
    try:
        yield amount
    finally:
        print(f"✅ Ресурс {resource_name} использован безопасно")

# Использование
print("=== УПРАВЛЕНИЕ РЕСУРСАМИ ФЕРМЫ ===")

with GreenhouseManager("Зимний сад") as greenhouse:
    print(f"🌡️ Температура в {greenhouse.name}: 25°C")
    print("🌿 Растения растут...")
    time.sleep(1)

print("\n")

with farm_resource("вода", 100) as water:
    print(f"💧 Поливаю растения используя {water}л воды")
    time.sleep(0.5)

print("\n🎉 Все ресурсы управлены безопасно!")`,
                    initialCode: `# Создай систему управления ресурсами
# 1. Реализуй класс с __enter__ и __exit__
# 2. Создай контекстный менеджер с @contextmanager
# 3. Протестируй безопасное использование ресурсов`,
                    checks: [
                        { keyword: "__enter__", points: 30, error: "Реализуй метод __enter__" },
                        { keyword: "__exit__", points: 30, error: "Реализуй метод __exit__" },
                        { keyword: "@contextmanager", points: 20, error: "Используй декоратор contextmanager" },
                        { keyword: "yield", points: 20, error: "Используй yield в контекстном менеджере" }
                    ],
                    farmUpdate: {
                        action: "resource_management",
                        safetyLevel: 95,
                        efficiency: 130,
                        visualEffect: "resource_monitor",
                        resources: { safety: 200, reliability: 150 }
                    }
                },

                // УРОКИ 21-27: БЫСТРЫЕ УРОКИ
                {
                    id: "lesson_21",
                    title: "Базы данных - Хранение данных фермы",
                    description: "Научись работать с данными фермы",
                    level: 11,
                    experience: 1100,
                    coins: 550,
                    task: "Создай систему хранения данных фермы в JSON формате.",
                    exampleCode: `import json

class FarmDatabase:
    def __init__(self):
        self.data = {
            "resources": {"water": 100, "coins": 500},
            "buildings": [],
            "crops": []
        }
    
    def save(self, filename):
        with open(filename, 'w') as f:
            json.dump(self.data, f, indent=2)
        print("💾 Данные сохранены")
    
    def load(self, filename):
        with open(filename, 'r') as f:
            self.data = json.load(f)
        print("📂 Данные загружены")

db = FarmDatabase()
db.save("farm_data.json")`,
                    initialCode: `# Создай систему хранения данных
# 1. Используй JSON для хранения
# 2. Реализуй сохранение и загрузку
# 3. Добавь обработку ошибок`,
                    farmUpdate: {action: "database_system", resources: {coins: 100}}
                },
                {
                    id: "lesson_22",
                    title: "Анализ данных - Статистика урожая",
                    description: "Анализируй данные урожайности",
                    level: 11,
                    experience: 1150,
                    coins: 575,
                    task: "Создай систему анализа статистики фермы.",
                    exampleCode: `class FarmAnalytics:
    def __init__(self, data):
        self.data = data
    
    def average_yield(self):
        if not self.data:
            return 0
        return sum(self.data) / len(self.data)
    
    def best_performing(self):
        return max(self.data) if self.data else 0

stats = FarmAnalytics([10, 15, 20, 25])
print(f"📊 Средняя урожайность: {stats.average_yield()}")`,
                    initialCode: `# Создай систему анализа
# 1. Реализуй расчет средней урожайности
# 2. Добавь поиск максимального значения
# 3. Проанализируй данные`,
                    farmUpdate: {action: "analytics_system", resources: {knowledge: 100}}
                },
                {
                    id: "lesson_23",
                    title: "Визуализация данных - Графики роста",
                    description: "Создавай графики для визуализации данных фермы",
                    level: 12,
                    experience: 1200,
                    coins: 600,
                    task: "Создай систему генерации текстовых графиков.",
                    exampleCode: `def create_bar_chart(data, max_width=20):
    max_value = max(data.values())
    
    for key, value in data.items():
        bar_width = int((value / max_value) * max_width)
        bar = "█" * bar_width
        print(f"{key:10} {bar} {value}")
    
    return "📈 График создан"

crops = {"пшеница": 100, "морковь": 75, "картофель": 50}
create_bar_chart(crops)`,
                    initialCode: `# Создай систему визуализации
# 1. Реализуй текстовые графики
# 2. Добавь масштабирование
# 3. Отобрази данные фермы`,
                    farmUpdate: {action: "visualization", resources: {presentation: 100}}
                },
                {
                    id: "lesson_24",
                    title: "API запросы - Рыночные цены",
                    description: "Получай данные о ценах на рынке",
                    level: 12,
                    experience: 1250,
                    coins: 625,
                    task: "Создай систему имитации API запросов для рынка.",
                    exampleCode: `import random

class MarketAPI:
    def __init__(self):
        self.prices = {
            "пшеница": random.randint(40, 60),
            "морковь": random.randint(30, 50),
            "картофель": random.randint(20, 40)
        }
    
    def get_price(self, crop):
        return self.prices.get(crop, 0)
    
    def update_prices(self):
        for crop in self.prices:
            self.prices[crop] += random.randint(-5, 5)

market = MarketAPI()
print(f"💰 Цена пшеницы: {market.get_price('пшеница')} монет")`,
                    initialCode: `# Создай систему API для рынка
# 1. Имитируй получение цен
# 2. Добавь обновление цен
# 3. Реализуй запросы данных`,
                    farmUpdate: {action: "market_api", resources: {coins: 150}}
                },
                {
                    id: "lesson_25",
                    title: "Регулярные выражения - Анализ логов",
                    description: "Используй регулярные выражения для анализа данных",
                    level: 13,
                    experience: 1300,
                    coins: 650,
                    task: "Создай систему парсинга логов фермы.",
                    exampleCode: `import re

class LogParser:
    def __init__(self):
        self.patterns = {
            'error': r'ERROR:.+',
            'warning': r'WARNING:.+',
            'harvest': r'Harvested:.+'
        }
    
    def parse_log(self, log_text):
        results = {}
        for name, pattern in self.patterns.items():
            matches = re.findall(pattern, log_text)
            results[name] = len(matches)
        return results

logs = "ERROR: No water\nHarvested: 10kg wheat\nWARNING: Low energy"
parser = LogParser()
print(f"📊 Анализ логов: {parser.parse_log(logs)}")`,
                    initialCode: `# Создай систему анализа логов
# 1. Используй регулярные выражения
# 2. Реализуй поиск паттернов
# 3. Анализируй логи фермы`,
                    farmUpdate: {action: "log_analysis", resources: {debugging: 100}}
                },
                {
                    id: "lesson_26",
                    title: "Многопроцессорность - Параллельные вычисления",
                    description: "Используй многопроцессорность для тяжелых вычислений",
                    level: 13,
                    experience: 1350,
                    coins: 675,
                    task: "Создай систему параллельных вычислений для фермы.",
                    exampleCode: `from multiprocessing import Pool

def process_field(field_data):
    return field_data * 2  # Имитация обработки

class ParallelProcessor:
    def process_fields(self, fields):
        with Pool() as pool:
            results = pool.map(process_field, fields)
        return sum(results)

fields = [10, 20, 30, 40, 50]
processor = ParallelProcessor()
result = processor.process_fields(fields)
print(f"⚡ Результат параллельной обработки: {result}")`,
                    initialCode: `# Создай систему параллельных вычислений
# 1. Используй multiprocessing.Pool
# 2. Реализуй обработку данных
# 3. Протестируй на данных фермы`,
                    farmUpdate: {action: "parallel_processing", resources: {speed: 300}}
                },
                {
                    id: "lesson_27",
                    title: "Кэширование - Оптимизация запросов",
                    description: "Ускоряй работу с помощью кэширования",
                    level: 14,
                    experience: 1400,
                    coins: 700,
                    task: "Создай систему кэширования для часто используемых данных.",
                    exampleCode: `class Cache:
    def __init__(self, max_size=100):
        self.cache = {}
        self.max_size = max_size
    
    def get(self, key):
        return self.cache.get(key)
    
    def set(self, key, value):
        if len(self.cache) >= self.max_size:
            # Удаляем самый старый ключ
            oldest = next(iter(self.cache))
            del self.cache[oldest]
        self.cache[key] = value
    
    def clear(self):
        self.cache.clear()

cache = Cache()
cache.set("weather", "sunny")
print(f"🌤️ Погода из кэша: {cache.get('weather')}")`,
                    initialCode: `# Создай систему кэширования
# 1. Реализуй класс Cache
# 2. Добавь ограничение размера
# 3. Протестируй кэширование данных`,
                    farmUpdate: {action: "caching_system", resources: {performance: 200}}
                },

                // ЗАВЕРШЕНИЕ ФАЙЛА
                {
                    id: "lesson_28",
                    title: "Финальный проект - Автономная ферма",
                    description: "Создай полностью автономную систему управления фермой",
                    level: 15,
                    experience: 5000,
                    coins: 5000,
                    theory: `<h2>🎓 Финальный проект!</h2><p>Объедини все знания для создания умной фермы.</p>`,
                    task: "Создай класс AutonomousFarm, который управляет всеми процессами автоматически.",
                    exampleCode: `class AutonomousFarm:
    def __init__(self):
        self.sensors = WeatherSensor()
        self.market = MarketAPI()
        self.robots = FarmRobots()
    
    def run_daily_cycle(self):
        weather = self.sensors.get_weather()
        prices = self.market.get_prices()
        
        if weather.should_water():
            self.robots.water_all()
        
        if prices.is_good_for_sell():
            self.robots.harvest_and_sell()

farm = AutonomousFarm()
farm.run_daily_cycle()
print("🤖 Автономная ферма работает!")`,
                    initialCode: `# Создай автономную ферму
# 1. Объедини все изученные концепции
# 2. Реализуй автоматическое управление
# 3. Протестируй систему`,
                    checks: [
                        { keyword: "class AutonomousFarm", points: 20 },
                        { keyword: "def __init__", points: 20 },
                        { keyword: "def run_daily_cycle", points: 20 },
                        { keyword: "self.sensors", points: 20 },
                        { keyword: "if ", points: 20 }
                    ],
                    farmUpdate: {
                        action: "autonomous_system",
                        autonomous: true,
                        efficiency: 100,
                        visualEffect: "future_farm",
                        resources: { coins: 5000, technology: 1000 }
                    }
                }
            ];
        }
    
        getAllLessons() {
            return this.lessons;
        }
    
        getLesson(lessonId) {
            return this.lessons.find(lesson => lesson.id === lessonId);
        }
    
        getLessonsByLevel(level) {
            return this.lessons.filter(lesson => lesson.level === level);
        }
    
        getNextLesson(currentLessonId) {
            const currentIndex = this.lessons.findIndex(lesson => lesson.id === currentLessonId);
            if (currentIndex < this.lessons.length - 1) {
                return this.lessons[currentIndex + 1];
            }
            return null;
        }
    
        getLessonCount() {
            return this.lessons.length;
        }
    
        getTotalExperience() {
            return this.lessons.reduce((sum, lesson) => sum + lesson.experience, 0);
        }
    
        getTotalCoins() {
            return this.lessons.reduce((sum, lesson) => sum + lesson.coins, 0);
        }
    }
    
    module.exports = Lessons;
